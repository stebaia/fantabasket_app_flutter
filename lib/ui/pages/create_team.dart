import 'package:flutter/material.dart';

class CreatTeamPage extends StatelessWidget {
  const CreatTeamPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('CreatTeamPage'),
      ),
    );
  }
}
